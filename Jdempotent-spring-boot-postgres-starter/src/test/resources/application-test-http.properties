# Test properties for HTTP integration tests
logging.level.root=INFO
logging.level.jdempotent=DEBUG
logging.level.org.springframework.web=DEBUG

# Disable response persistence to avoid serialization issues with ResponseEntity
jdempotent.cache.persistReqRes=false
