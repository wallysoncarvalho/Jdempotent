# Test properties for Kafka integration tests
logging.level.root=INFO
logging.level.jdempotent=DEBUG
logging.level.org.springframework.kafka=DEBUG

# Disable response persistence to avoid serialization issues
jdempotent.cache.persistReqRes=false
